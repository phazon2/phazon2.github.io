# Extracted third-party video content — derivative material, kept out of a
# public repo. The tooling that produces it is versioned; the output is not.
corpus/
outreach/*.md
!outreach/START-HERE.md

__pycache__/
*.pyc

# Generated episode output — MP4s, frames, and the API response cache.
# Regenerable from episodes/*.json, and too large for a public repo.
build/
