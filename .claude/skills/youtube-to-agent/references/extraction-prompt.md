Extract the *mechanism* of this tutorial, not a summary of it.

Report, with a timestamp for each item:

1. **Commands** — every command typed or shown, verbatim, including flags.
2. **Files** — every file path created or edited, and its contents as shown.
3. **Named things** — tools, plugins, repos, packages, models, APIs, in the
   exact form given (e.g. `owner/repo`, `plugin@marketplace`).
4. **Order** — the sequence of steps, and any step explicitly called out as
   order-dependent.
5. **Decisions** — every point where the presenter chooses between options,
   and the stated reason.
6. **Shown but not said** — anything visible only on screen (terminal output,
   config, UI state) that is never spoken aloud.
7. **Said but not shown** — anything asserted verbally with no on-screen
   evidence.
8. **Assumed** — prerequisites, credentials, or prior setup the tutorial takes
   for granted without demonstrating.

Rules:

- Quote exactly. Do not normalize, correct, or modernize a command.
- If something is unreadable or ambiguous, say `UNCLEAR` and give the
  timestamp. Never fill a gap with what a step like this usually looks like.
- Distinguish what the video *shows* from what you *infer*. Label inferences.
